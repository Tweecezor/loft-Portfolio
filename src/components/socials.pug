mixin socials(mod)
  ul(class='socials__list socials__list--' + mod)
    -var items = [{name:"insta",href:'#'},{name:"vk",href:'#'},{name:"github",href:'#'}]
    each item in items
      li.socials__item
        a(href="item.href").socials__link
          +icon(`${item.name}`,'socials-link')